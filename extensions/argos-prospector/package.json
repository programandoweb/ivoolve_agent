{
  "name": "@ivoolve/argos-prospector-extension",
  "private": true,
  "version": "1.0.0",
  "description": "Argos Browser Worker: Google Maps visual prospecting through Socket.IO",
  "type": "module",
  "scripts": { "build": "node scripts/build.mjs", "watch": "node scripts/build.mjs --watch" },
  "dependencies": { "socket.io-client": "^4.8.1" },
  "devDependencies": { "esbuild": "^0.25.0", "sharp": "^0.33.5" }
}
